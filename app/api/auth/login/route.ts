import { createClient } from "@supabase/supabase-js";

export async function POST(request:Request){const body=await request.json();const email=String(body.email||"").trim(),password=String(body.password||"");if(!email||!password)return Response.json({ok:false,error:"Enter email and password."},{status:400});const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY||process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY;if(!url||!key)return Response.json({ok:false,error:"Supabase authentication is not configured."},{status:503});const client=createClient(url,key,{auth:{persistSession:false,autoRefreshToken:false}});const {data,error}=await client.auth.signInWithPassword({email,password});if(error||!data.session)return Response.json({ok:false,error:"Email or password was not accepted."},{status:401});const role=String(data.user.user_metadata?.role||"analyst");const response=Response.json({ok:true,user:{id:data.user.id,email:data.user.email,role}});response.headers.append("Set-Cookie",`dr_session=${data.session.access_token}; Path=/; HttpOnly; Secure; SameSite=Lax; Max-Age=${data.session.expires_in}`);return response}
